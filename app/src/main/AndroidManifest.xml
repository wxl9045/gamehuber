<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="safe.com.gamehuber">
    <!-- 访问网络 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="safe.com.gamehuber.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="safe.com.gamehuber.mvp.page.LoginActivity">
        </activity>
        <activity android:name="safe.com.gamehuber.mvp.page.ResetPasswordActivity"/>
        <activity android:name="safe.com.gamehuber.mvp.page.CreateAccountActivity"/>
        <activity android:name="safe.com.gamehuber.mvp.page.VideoDetailActivity"/>
        <activity android:name="safe.com.gamehuber.mvp.page.UserDetailActivity"/>
        <activity android:name="safe.com.gamehuber.mvp.page.RichEditorActivity"/>
        <activity android:name="safe.com.gamehuber.mvp.page.SendVideoActivity"/>
    </application>

</manifest>